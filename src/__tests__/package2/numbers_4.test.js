const { isPrime, getAllPrimes } = require("../../package2/numbers_3");

describe("Testing Numbers 3", () => {

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });


  it("Get all Prime Numbers 100", () => {
    const x = getAllPrimes(100);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 1000", () => {
    const x = getAllPrimes(1000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 10000", () => {
    const x = getAllPrimes(10000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 100000", () => {
    const x = getAllPrimes(100000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 200", () => {
    const x = getAllPrimes(200);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 2000", () => {
    const x = getAllPrimes(2000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 20000", () => {
    const x = getAllPrimes(20000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 300", () => {
    const x = getAllPrimes(300);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 3000", () => {
    const x = getAllPrimes(3000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 30000", () => {
    const x = getAllPrimes(30000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 400", () => {
    const x = getAllPrimes(400);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 4000", () => {
    const x = getAllPrimes(4000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 40000", () => {
    const x = getAllPrimes(40000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 500", () => {
    const x = getAllPrimes(500);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 5000", () => {
    const x = getAllPrimes(5000);
    expect(x.length > 0).toBe(true);
  });

  it("Get all Prime Numbers 50000", () => {
    const x = getAllPrimes(50000);
    expect(x.length > 0).toBe(true);
  });

});
